language: ruby
cache:    bundler
script:   bundle exec rake test
sudo:     false

before_install:
  - gem update --system
  - gem pristine bundler

rvm:
  - 2.2.9
  - 2.3.6
  - 2.4.3
  - 2.5.0
  - ruby-head
  - jruby-9.1.16.0
  - jruby-head

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-9.1.16.0
    - rvm: jruby-head
